const useLogin = () => {
  //   const { login } = useAuth();
  //   const { setToken } = useToken();

  const handleLogin = async () => {
    console.log("login");
  };

  return { handleLogin };
};

export default useLogin;
