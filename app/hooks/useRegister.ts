const useRegister = () => {
  //   const { login } = useAuth();
  //   const { setToken } = useToken();

  const handleRegister = async () => {
    console.log("login");
  };

  return { handleRegister };
};

export default useRegister;
